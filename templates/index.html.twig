<html>
<head>
    <title>Evil Sudoku</title>
    <link rel="icon" type="image/x-icon" href="/favicon.png">
    <style>
        table {
            border-spacing: 0;
            border-color: #1b1e21;
            border-style: solid;
        }
        td {
            padding: 0;
            text-align: center;
            height: 60px;
            width: 60px;
            border: 1px solid #2e3436;
            font-size: 36px;
        }
        tr {
            padding: 0;
            margin: 0;
        }
        .container {
            width: 100%;
        }
        .sudoku-grid {
            margin-left: auto;
            margin-right: auto;
        }
        .border-right {
            border-right: 2px solid #2e3436;

        }
        .border-bottom {
            border-bottom: 2px solid #2e3436;

        }
        .sudoku-col {
            border: none;
            width: 100%;
            height: 100%;
            color: #005cbf;
            font-size: 36px;
            text-align: center;
            outline: none;
            caret-color: transparent;
        }
        .sudoku-col:focus {
            background: #b8daff;
        }
    </style>
</head>
<body>
<header>
    <img src="/images/evil-sudoku-logo-90x90.png">
    <h1>Evil Sudoku</h1>
</header>

<div class="container">
    <table class="sudoku-grid">
        {% for rowIndex, row in grid %}
            <tr>
                {% for index, col in row %}
                    <td
                            class="
                               {{ ((index + 1) % block_size) == 0 ? 'border-right ': ''}}
                               {{ ((rowIndex + 1) % block_size) == 0 ? 'border-bottom': ''}}
                            "
                    >
                        {% if ' ' == col %}
                            <input class="sudoku-col" type="text" maxlength="1">
                        {% else %}
                            {{ col }}
                        {% endif %}
                    </td>
                {% endfor %}
            </tr>
        {% endfor %}

    </table>

</div>
</body>
</html>